# Core NLP (lightweight only — Log16)
spacy>=3.7.0                  # Primary NER (en_core_web_sm, ~15MB)
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl

# Feature engineering + aggregation
numpy>=1.26.0
scikit-learn>=1.4.0           # LogisticRegression intensity scorer (optional)

# API layer
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Storage
motor>=3.4.0                  # async MongoDB driver
pymongo>=4.7.0

# Geo / Routing
osmnx>=1.9.0                  # road routing
searoute>=1.2.0               # sea routing
shapely>=2.0.0                # buffer + geo operations
geopy>=2.4.0                  # Nominatim geocoding (Log3)

# Ingestion
httpx>=0.27.0                 # async HTTP for GDELT + RSS + APIs (Log5)
aiofiles>=23.2.1

# Observability
structlog>=24.1.0
prometheus-client>=0.20.0

# Utilities
python-dotenv>=1.0.0          # .env file loading

# Dev / Test
pytest>=8.2.0
pytest-asyncio>=0.23.0

# Log16: REMOVED transformers, torch, and all HuggingFace dependencies.
# These caused OOM on Render Free Tier (512MB RAM).
# Classification now uses keyword heuristics; NER uses spaCy only.
